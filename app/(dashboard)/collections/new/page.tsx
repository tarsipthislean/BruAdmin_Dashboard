import CollectionForm from "@/components/collections/CollectionForm";

// ฟังก์ชัน CreateCollection สำหรับสร้างลูกค้าใหม่
const CreateCollection = () => {
  return (
    <CollectionForm />
  );
};

export default CreateCollection;
